# Runtime -- mirrors `dependencies` in pyproject.toml exactly.
# `test/test_packaging.py` asserts the two agree; they disagreed on the numpy
# floor (2.4.0 here against 2.0.0 there), and this file is what a developer
# installs from.
numpy>=2.0.0
torann>=0.2.1

# Lint toolchain, pinned to the versions .github/workflows/main.yml installs.
# Unpinned, a ruff release lands new rules in CI that the local gate never saw.
ruff==0.16.1
basedpyright==1.39.8
vulture==2.16

# Docs and the example plots. Not needed to use the library.
pdoc>=16.0.0
matplotlib>=3.10.8
